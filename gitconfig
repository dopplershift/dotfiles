[alias]
	st = status
	ci = commit
	ds = diff --staged
	co = checkout
	unstage = reset HEAD --
	clear = checkout --
	ribbon = tag --force _ribbon origin/edge5
	catchup = log --stat --reverse --topo-order _ribbon..origin/edge5
[user]
	name = Ryan May
	email = ryan.may@eecradar.com
[color]
	ui = auto
[merge]
	tool = meld
[push]
	default = tracking
